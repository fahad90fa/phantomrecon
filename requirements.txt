# ── Core async HTTP ──────────────────────────────────────────────────────────
aiohttp>=3.9.0
aiohttp-socks>=0.8.4
aiofiles>=23.2.1
httpx[http2]>=0.27.0

# ── GUI ───────────────────────────────────────────────────────────────────────
PyQt6>=6.5.0

# ── CLI / TUI ─────────────────────────────────────────────────────────────────
click>=8.1.7
rich>=13.7.0
colorama>=0.4.6

# ── HTML parsing ─────────────────────────────────────────────────────────────
beautifulsoup4>=4.12.3
lxml>=5.1.0

# ── Templating / reporting ───────────────────────────────────────────────────
jinja2>=3.1.3

# ── DNS ───────────────────────────────────────────────────────────────────────
dnspython>=2.6.1

# ── Cryptography (john.py AES key wrap, SSL analysis) ────────────────────────
cryptography>=42.0.0
certifi>=2024.2.2

# ── Config / serialisation ───────────────────────────────────────────────────
PyYAML>=6.0.1
toml>=0.10.2

# ── HTTP utilities ────────────────────────────────────────────────────────────
urllib3>=2.2.0
requests>=2.31.0
brotli>=1.1.0

# ── Misc utilities ────────────────────────────────────────────────────────────
tldextract>=5.1.2
fake-useragent>=1.4.0
tabulate>=0.9.0
python-dateutil>=2.9.0
asyncio-throttle>=1.0.2

# ── Brute-force / protocol modules (hydra.py) ─────────────────────────────────
paramiko>=3.4.0
impacket>=0.11.0
pymysql>=1.1.0
psycopg2-binary>=2.9.9
pymongo>=4.6.0
ldap3>=2.9.1

# ── Cloud storage scanning (s3scanner.py) ─────────────────────────────────────
boto3>=1.34.0

# ── Web exploitation (websploit.py / sqli_advanced.py) ────────────────────────
pycryptodome>=3.20.0
